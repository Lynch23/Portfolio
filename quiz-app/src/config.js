const API_URL = `https://opentdb.com/api.php?`;
const AMOUNT = `amount=`;
const CATEGORY = `&category=`;
const LEVEL = `&difficulty=`;

export {
    API_URL,
    AMOUNT,
    CATEGORY,
    LEVEL
}

